exec:
  default:
    targets:
      sceanrio: smtp
      testCount: 1
      testDuration: 10
      endlessRetryLoop: false

scenarios:
  smtp:
    maxNumSimlTrxn: 10
    maxNumSimlConn: 10
    numTrxnPerSec:  10
    # numTrxnInterval: 10
    params:
      from: [[pacmain, pacmain], range1]
      rcpt: [[pacmain], range1]
    args:
	  range1: [permutation, 0, 1000]
